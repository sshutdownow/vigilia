apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-service
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: {{ (index .Values "sausage-store").frontend.service.port | default 80 }}
      targetPort: 80