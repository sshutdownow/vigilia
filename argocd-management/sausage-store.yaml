apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sausage-store
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://cloud-services-engineer.gitlab.yandexcloud.net"
    path: "deploy-yandex-cloud"
    targetRevision: "master"
    helm:
      releaseName: sausage-store
      parameters:
        - name: "sausage-store.ingress.gwin_ip"
          valueFrom:
            configMapKeyRef:
              name: infra-info
              key: gwin_ip
        - name: "sausage-store.ingress.gwin_sg"
          valueFrom:
            configMapKeyRef:
              name: infra-info
              key: gwin_sg
        - name: "sausage-store.ingress.certificate_id"
          valueFrom:
            configMapKeyRef:
              name: infra-info
              key: certificate_id
  destination:
    server: "https://kubernetes.default.svc"
    namespace: "sausage-store"
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions: ["CreateNamespace=true"]
